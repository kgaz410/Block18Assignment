// Builing a function that returns the product of two numbers.

// Expectations:
// 1) Multiply two input numbers together.
// 2) The function accepts the two numbers as arguments.
// 3) Returns a single number.
// 4) The return is the product of the two input numbers.

const product = (a, b) => {
    return a * b
}


module.exports = product